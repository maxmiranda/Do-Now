let username = "ty.dtm_"; // String
let followerCount = 1000000; // Number 

let name = "Tyra";

// This means that we're changing the username on all places that have the name class on them
$(".name").text(username);

// This means that we're changing the follower count on all the places that have the followers class on them 
$(".followers").text(followerCount);
